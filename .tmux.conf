# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'dracula/tmux'
set -g @continuum-restore on
set -g @plugin 'nhdaly/tmux-better-mouse-mode'

# General
set -g base-index 1
set -g pane-base-index 1
set -g renumber-windows on

# Terminal settings
set -s default-terminal 'tmux-256color'
set -as terminal-features ',xterm-256color:RGB'
set -s extended-keys on
set -as terminal-features '*:extkeys'

# Dracula theme
set -g @dracula-show-left-icon window
set -g @dracula-plugins "git"

# Key bindings
bind-key -n Home send-keys Home
bind-key -n End send-keys End

# Mouse support
set -g mouse on
set -gq allow-passthrough all
set -g @scroll-speed-num-lines-per-scroll 5
set -g @scroll-without-changing-pane on
set -g @emulate-scroll-for-no-mouse-alternate-buffer on

# Shell
set -g default-shell /usr/bin/zsh

# Nested tmux toggle (F12): pass all keys to inner tmux, dim status bar
# https://gist.github.com/samoshkin/05e65f7f1c9b55d3fc7690b59d678734
bind -T root F12 set prefix None \; set key-table off \; set status-style "fg=#6272a4,bg=#44475a" \; refresh-client -S
bind -T off F12 set -u prefix \; set -u key-table \; set -u status-style \; refresh-client -S

# Window/pane titles
set -g allow-rename off
set -g automatic-rename off
bind T command-prompt -p "Pane title:" "select-pane -T '%%'"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# After plugins load (dracula overrides pane-border)
set -g pane-border-status top
set -g pane-border-format "#{pane_title}"

# Reset pane title for new panes
set-hook -g after-new-window 'select-pane -T ""'
set-hook -g after-split-window 'select-pane -T ""'
